RewriteEngine On

# Seuls les fichiers réels (assets, uploads, favicons…) sont servis directement.
# Les dossiers comme /admin/ ne doivent PAS court-circuiter le routeur.
RewriteCond %{REQUEST_FILENAME} -f
RewriteRule ^ - [L]

# Tout le reste → front controller
RewriteRule ^ index.php [QSA,L]
